name: Heroku Awake

on:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  keep_prod_awake:
    runs-on: ubuntu-latest

    steps:
      - uses: mikeesto/heroku-awake@1.0.0
        with:
          URL: "https://tech-stack-portal-prod.herokuapp.com/"

  keep_dev_awake:
    runs-on: ubuntu-latest

    steps:
      - uses: mikeesto/heroku-awake@1.0.0
        with:
          URL: "https://tech-stack-portal-dev.herokuapp.com/"